# Example of [angular-mount-react](https://www.npmjs.com/package/angular-mount-react)

you can find the usage example in `src/index.js` you will also need look at `index.html` to get a full understending
